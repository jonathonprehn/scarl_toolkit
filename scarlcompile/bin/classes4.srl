
class A {
	int field;
	int funcOfClass() {
		field = field + 2;	
	}
}

class B extends A {
	int moreData;
}

int stuff() {
	B bee = allocate B();
	//bee.moreData = 20; // not working
}

void main() {
	A generalCase = allocate B();
	a.funcOfclass();
	
	int pointer w = allocate int(10);
	int g = deref w;
	delete w;
	char c = 'y';
}