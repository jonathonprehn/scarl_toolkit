
class A {
	int field;
	int funcOfClass() {
		field = field + 2;	
	}
}

class B extends A {
	int moreData;
}

void main() {
	A generalCase = allocate B();
	a.funcOfclass();
	
	int pointer w = allocate int(10);
	int g = deref w;
	delete w;
	char c = 'y';
}